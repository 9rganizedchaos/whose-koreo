/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "img-dev.diowith.me",
          "image.diowith.me",
          "img-dev.diodio.xyz",
        ],
      },
};

export default nextConfig;
